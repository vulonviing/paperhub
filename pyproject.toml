[build-system]
requires = ["hatchling>=1.18"]
build-backend = "hatchling.build"

[project]
name = "paperhub"
version = "0.1.0"
description = "Fetch HuggingFace Daily Papers and produce English, Jupyter-friendly AI summaries with optional Turkish output — one paper, one agent."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
    { name = "PaperHub maintainers" },
]
keywords = ["huggingface", "papers", "arxiv", "llm", "summarization"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "httpx>=0.27",
    "tenacity>=8",
    "beautifulsoup4>=4.12",
    "pypdf>=4",
    "pdfplumber>=0.11",
    "pydantic>=2",
    "pydantic-settings>=2",
    "openai>=1.40",
    "rich>=13",
    "ipython>=8",
    "python-dateutil>=2.9",
    "platformdirs>=4",
]

[project.optional-dependencies]
anthropic = ["anthropic>=0.40"]
google = ["google-genai>=0.3"]
dev = [
    "pytest>=8",
    "pytest-asyncio>=0.23",
    "ruff>=0.4",
    "mypy>=1.8",
    "build>=1.2",
    "types-python-dateutil",
]

[project.scripts]
paperhub = "paperhub.interactive_cli:main"

[project.urls]
Homepage = "https://github.com/paperhub/paperhub"
Issues = "https://github.com/paperhub/paperhub/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/paperhub"]

[tool.hatch.build.targets.sdist]
include = [
    "src/paperhub",
    "tests",
    "docs",
    "examples",
    ".env.example",
    "LICENSE",
    "README.md",
    "pyproject.toml",
]
exclude = [
    ".claude",
    "starter-template",
    "docs/PUBLISHING.md",
    "**/__pycache__",
    "**/*.pyc",
]

[tool.ruff]
line-length = 100
target-version = "py310"
extend-exclude = ["build", "dist", ".venv"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP", "N", "C4", "SIM"]
ignore = [
    "E501",  # line length handled by formatter
    "B008",  # function calls in argument defaults (Click decorators)
    "N818",  # exception name suffix
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["B011", "N802", "N803"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-q"
filterwarnings = [
    "ignore::DeprecationWarning",
    "ignore::PendingDeprecationWarning",
]

[tool.mypy]
python_version = "3.10"
strict_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
no_implicit_optional = true
ignore_missing_imports = true
disallow_untyped_defs = false
follow_imports = "silent"
files = ["src", "tests"]

[[tool.mypy.overrides]]
module = "tests.*"
disallow_untyped_defs = false
check_untyped_defs = false
